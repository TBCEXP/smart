# Phase 4 — 印刷前稿 AI（已移除）

> 产品定位调整为 **专注 B2B 搜客**。前稿 OCR / 条码 / 像素比对已从 v2.1.0 精简版移除，以减小 Docker 镜像与 VPS 内存占用。

原功能：Tesseract OCR、ZBar 条码、Pillow 像素 diff、前稿比对任务。

如需恢复，见 git 历史 `prepress.py` / `phase4_verify.sh`。
